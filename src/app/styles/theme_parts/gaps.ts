/**
 * @typedef ThemeGaps
 */
export type ThemeGaps = {
    xl: string;
    lg: string;
    md: string;
    sm: string;
    none: string;
};

/**
 * Theme gaps list
 */
export const gaps: ThemeGaps = {
    xl: '2rem',
    lg: '1.5rem',
    md: '1rem',
    sm: '0.5rem',
    none: '0px',
};
