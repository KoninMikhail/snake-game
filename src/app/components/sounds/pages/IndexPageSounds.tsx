import { UISounds } from '@sounds/schemas/UISounds/UISounds';
import { SideSounds } from '@sounds/schemas/SideSounds/SideSounds';

export const IndexPageSounds = () => {
    return (
        <>
            <UISounds />
            <SideSounds />
        </>
    );
};
