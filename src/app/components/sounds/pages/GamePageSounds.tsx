import { UISounds } from '@sounds/schemas/UISounds/UISounds';
import { GameSounds } from '@sounds/schemas/GameSounds/GameSounds';

export const GamePageSounds = () => {
    return (
        <>
            <UISounds />
            <GameSounds />
        </>
    );
};
