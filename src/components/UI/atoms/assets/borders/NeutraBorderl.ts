import styled from 'styled-components';

// eslint-disable-next-line import/no-unused-modules
export const NeutralBorder = styled.div`
  width: inherit;
  height: inherit;
  border: 5px solid #fff;
`;
